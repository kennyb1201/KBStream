<?xml version="1.0" encoding="utf-8"?>
<!-- Next episode: skip-forward, the mark the button used to draw as the
     "⏭" character. Drawn as geometry for the same reason the source button's
     glyph was: a font's own symbol changes shape (and weight) with the system
     font, so the bar's buttons never quite matched each other. Carries
     kb_text_hi (#FFF3EFE4) baked in - no layout here tints an ImageView. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFF3EFE4"
        android:pathData="M7,5.6L16.2,12L7,18.4Z" />
    <path
        android:fillColor="#FFF3EFE4"
        android:pathData="M17.4,5.6h2.6v12.8h-2.6z" />
</vector>
